import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.agenthum.propertyanalyzer',
  appName: 'Property Analyzer',
  webDir: 'build'
};

export default config;
